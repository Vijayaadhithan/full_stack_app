{
  "//": "Copy this file to config/network-config.json and adjust values per environment.",
  "frontendUrl": "${FRONTEND_URL}",
  "appBaseUrl": "${APP_BASE_URL}",
  "allowedOrigins": [
    "${FRONTEND_URL}",
    "${APP_BASE_URL}"
  ],
  "devServerHost": "${DEV_SERVER_HOST}",
  "devServerPort": 5173,
  "devServerHmrHost": "${DEV_SERVER_HOST}",
  "devServerHmrPort": 5173,
  "devServerHmrProtocol": "ws",
  "apiProxyTarget": "${APP_BASE_URL}"
}
